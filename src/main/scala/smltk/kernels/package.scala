package smltk.kernels

object kernels {
  def polynomial_kernel(degree: Int) = {
    def withData(x: DenseVector[Double], y: DenseVector[Double]) = {
    }
  }

  def gaussian_kernel(sigma: Int) = {
    def withData(x: DenseVector[Double], y: DenseVector[Double]) = {
    }
  }
}
