package smltk.kernels

import breeze.linalg._

object kernels {
  def polynomial_kernel(degree: Int)(x: DenseVector[Double], y: DenseVector[Double]) = {
  }

  def gaussian_kernel(sigma: Int)(x: DenseVector[Double], y: DenseVector[Double]) = {
  }
}
